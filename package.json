{
	"name": "template-worker-router",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy index.js",
		"dev": "wrangler dev index.js --local true",
		"dev:init": "wrangler d1 execute DB --file init.sql --local"
	},
	"dependencies": {
		"itty-router": "^2.6.1"
	},
	"devDependencies": {
		"wrangler": "^3.0.0"
	}
}
